---
apiVersion: v1
kind: Service
metadata:
  name: twirpt
  labels:
    app: twirpt
spec:
  selector:
    app: twirpt
  ports:
  - port: 8080
  type: LoadBalancer

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: twirpt
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: twirpt
    spec:
      containers:
      - name: twirpt
        image: twirpt
        ports:
        - containerPort: 8080
        env:
          - name: "PORT"
            value: "8080"
